{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build OpenGL",
      "type": "shell",
      "command": "clang++",
      "args": [
        "-std=c++17",
        "-Wall",
        "${workspaceFolder}/*.cpp",
        "-framework", "OpenGL",
        "-framework", "GLUT",
        "-o", "${workspaceFolder}/build/Debug/outDebug"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
